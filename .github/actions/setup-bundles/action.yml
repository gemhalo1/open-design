name: Setup bundles
description: Build bundles required by workspace validation jobs

runs:
  using: composite
  steps:
    - name: Build bundles
      shell: bash
      run: |
        pnpm --filter @open-design/daemon build
        pnpm --filter @open-design/desktop build
        pnpm --filter @open-design/web build:sidecar
